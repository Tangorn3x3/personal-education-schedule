/*
import { LoaderPlugin } from 'vue-google-login';
import Vue from "vue";
import nuxtConfig from "../../nuxt.config";

Vue.use(LoaderPlugin, {
    client_id: nuxtConfig.auth.strategies.google.clientId,
});
*/


/*import Vue from 'vue'
import GAuth from 'vue-google-oauth2'
import nuxtConfig from "../../nuxt.config";

const gauthOption = {
    clientId: nuxtConfig.auth.strategies.google.clientId,
    scope: 'profile email',
    prompt: 'select_account'
}
Vue.use(GAuth, gauthOption)*/
